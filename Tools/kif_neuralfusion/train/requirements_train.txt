# GPU training libraries for the KIF NeuralFusion LoRA.
#
# IMPORTANT: this file does NOT list torch on purpose. Installing it here would
# let pip replace your CUDA torch with a CPU-only wheel (and break other apps).
# The setup uses an ISOLATED env and installs CUDA torch separately, matched to
# your GPU -- just run:  python bootstrap.py train   (or the Setup .bat).
#
# These libraries do not pin or move torch:
diffusers>=0.27
transformers>=4.38
accelerate>=0.27
peft>=0.10
safetensors>=0.4
numpy>=1.21
pillow>=9.0
# (torch + torchvision are installed by bootstrap.py from the pytorch CUDA index)
# 'datasets' is intentionally omitted (only needed for diffusers' official
#  script, and it can downgrade fsspec). train_lora.py does not use it.
